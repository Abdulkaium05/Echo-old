
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEMO MODE / FIREBASE DISCONNECTED
    //
    // If this app were connected to a live Firebase project,
    // these rules would need to be properly secured.
    // For a disconnected demo or local emulator testing,
    // allowing all reads and writes can simplify development.
    //
    // For a production app, NEVER use rules this open.
    //
    // Example of more secure rules (adapt as needed):
    //
    // match /users/{userId} {
    //   allow read: if request.auth != null;
    //   allow write: if request.auth != null && request.auth.uid == userId;
    // }
    //
    // match /chats/{chatId} {
    //   allow read, write: if request.auth != null && request.auth.uid in resource.data.participants;
    //   match /messages/{messageId} {
    //     allow read, write: if request.auth != null && request.auth.uid in get(/databases/$(database)/documents/chats/$(chatId)).data.participants;
    //   }
    // }

    // For Demo: Allow all reads and writes to all paths
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
